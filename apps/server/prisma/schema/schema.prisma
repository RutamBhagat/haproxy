generator client {
  provider = "prisma-client"
  output   = "../generated"
  moduleFormat = "esm"
}

generator kysely {
  provider = "prisma-kysely"
  output   = "../../src/db"
  fileName = "types.ts"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}
